import os.path
import freertos

Import('env')
Import('port')
Import('mem_mang')

freertos_env = env.Clone()
freertos_env.AppendUnique(CPPPATH=freertos.includes('.', port))

libfreertos = freertos_env.StaticLibrary(freertos.sources('.', port, mem_mang))

Return('libfreertos')
